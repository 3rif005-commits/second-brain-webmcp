# Dependencies for main_slim.py — the /db engine and notes routes only.
# Deliberately omits pymupdf, trafilatura, yt-dlp, python-pptx, python-docx,
# google-genai and openai: nothing reachable from the WebMCP tools imports
# them. See main_slim.py for when to use this.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
supabase>=2.10.0
asyncpg>=0.30.0
apscheduler>=3.10.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
PyYAML>=6.0.0
